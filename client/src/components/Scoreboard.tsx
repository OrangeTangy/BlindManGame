// Legacy component — the old per-round scoreboard. Kept as a no-op stub
// so any stale imports don't break the build. The gauntlet version of this
// lives in components/RaceProgress.tsx.
export default function Scoreboard() {
  return null;
}
