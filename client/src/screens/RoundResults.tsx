// Legacy screen — replaced by the inline stage-end toast in Game.tsx now that
// the gauntlet auto-advances between minigames. Kept as a harmless stub so
// no stale import path breaks.
export default function RoundResults() {
  return null;
}
