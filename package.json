{
  "name": "blindman-game",
  "private": true,
  "version": "1.0.0",
  "description": "Blind & Guide — a communication-driven party minigame for duos",
  "workspaces": ["shared", "server", "client"],
  "scripts": {
    "build:shared": "npm run build -w shared",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "dev": "npm run build:shared && concurrently -n server,client -c cyan,magenta \"npm run dev:server\" \"npm run dev:client\"",
    "build": "npm run build:shared && npm run build -w server && npm run build -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}
